{
  "name": "commonthread-webmcp",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal human-visible shared thread exposed through WebMCP.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
